<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 1) Пустой шаблон: отключает любую красную рамку у контейнеров -->
    <ControlTemplate x:Key="NoErrorTemplate">
        <AdornedElementPlaceholder />
    </ControlTemplate>

    <!-- 2) Шаблон для полей: подсветка + текст ошибки под контролом -->
    <ControlTemplate x:Key="FieldErrorTemplate">
        <StackPanel>
            <AdornedElementPlaceholder />
            <TextBlock Margin="4,2,0,0"
                       Foreground="Red"
                       FontSize="12"
                       Text="{Binding [0].ErrorContent}" />
        </StackPanel>
    </ControlTemplate>

    <!-- Гасим подсветку у ВСЕХ контейнеров -->
    <Style TargetType="Panel">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <Style TargetType="Border">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <!-- Важное: контейнеры, которые являются Control -->
    <Style TargetType="UserControl">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <Style TargetType="Window">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <Style TargetType="ContentControl">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <Style TargetType="ContentPresenter">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource NoErrorTemplate}" />
    </Style>

    <!-- Включаем красивую подсветку для инпутов (если нет своих стилей, они подхватятся) -->
    <Style TargetType="TextBox">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource FieldErrorTemplate}" />
        <Style.Triggers>
            <Trigger Property="Validation.HasError"
                     Value="True">
                <Setter Property="BorderBrush"
                        Value="Red" />
                <Setter Property="BorderThickness"
                        Value="2" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource FieldErrorTemplate}" />
        <Style.Triggers>
            <Trigger Property="Validation.HasError"
                     Value="True">
                <Setter Property="BorderBrush"
                        Value="Red" />
                <Setter Property="BorderThickness"
                        Value="2" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>