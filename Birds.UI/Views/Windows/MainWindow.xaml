<Window
    x:Class="Birds.UI.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Birds.UI.ViewModels"
    Width="1400"
    MinWidth="1000"
    MinHeight="650"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
    Background="Transparent"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Border Background="#272544">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Left">

                <RadioButton
                    Command="{Binding Path=Navigation.NavigateToTypeCommand,
                                      Mode=OneWay}"
                    CommandParameter="{x:Type vm:AddBirdViewModel}"
                    IsChecked="True"
                    Style="{StaticResource MenuButtonTheme}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Add bird" />
                        <Image
                            Width="20"
                            Margin="4 2 4 0"
                            Source="/Birds.UI;component/wwwroot/Images/add.png" />
                    </StackPanel>
                </RadioButton>

                <RadioButton
                    Command="{Binding Path=Navigation.NavigateToTypeCommand,
                                      Mode=OneWay}"
                    CommandParameter="{x:Type vm:BirdListViewModel}"
                    Style="{StaticResource MenuButtonTheme}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Birds" />
                        <Image
                            Width="20"
                            Margin="4 2 4 0"
                            Source="/Birds.UI;component/wwwroot/Images/list.png" />
                    </StackPanel>
                </RadioButton>

                <RadioButton
                    Content="Import"
                    Style="{StaticResource MenuButtonTheme}" />

            </StackPanel>

            <ContentControl
                Grid.Row="1"
                Grid.Column="1"
                Margin="20"
                Content="{Binding Path=Navigation.Current}" />
        </Grid>
    </Border>
</Window>
