<UserControl
    x:Class="Birds.UI.Views.UserControls.BirdsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Birds.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:Birds.UI.Services.Localization"
    xmlns:dto="clr-namespace:Birds.Application.DTOs;assembly=Birds.Application"
    xmlns:local="clr-namespace:Birds.UI.Views.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Birds.UI.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:BirdListViewModel}"
    d:DesignHeight="720"
    d:DesignWidth="980"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Birds.UI;component/Views/UserControls/Resources/BirdsViewResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="24,12,24,24">
        <Border
            MaxWidth="980"
            Padding="28"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0"
                        Margin="0,0,12,0"
                        MinWidth="200"
                        Style="{StaticResource ArchiveSearchBoxStyle}"
                        Tag="{Binding ClearSearchCommand}"
                        Text="{Binding SearchText, TargetNullValue='', UpdateSourceTrigger=PropertyChanged}" />

                    <Border
                        Grid.Column="1"
                        Margin="0,0,12,0"
                        MinHeight="46"
                        MinWidth="116"
                        Padding="12,0"
                        VerticalAlignment="Center"
                        Background="{StaticResource InsetBackgroundBrush}"
                        BorderBrush="{StaticResource FieldBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource MutedTextBrush}"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Text="{loc:Loc Key=BirdList.CountTitle}" />
                            <TextBlock
                                Grid.Column="1"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource PrimaryTextBrush}"
                                FontSize="18"
                                FontWeight="Bold"
                                Text="{Binding BirdCount, Mode=OneWay}" />
                        </Grid>
                    </Border>

                    <ComboBox
                        Grid.Column="2"
                        MinWidth="176"
                        MaxWidth="220"
                        DisplayMemberPath="Display"
                        ItemsSource="{Binding Filters}"
                        SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                        Style="{StaticResource ArchiveFilterComboBoxStyle}" />

                    <Button
                        Grid.Column="3"
                        Margin="14,0,0,0"
                        Command="{Binding ReloadBirdsCommand}"
                        Content="{loc:Loc Key=BirdList.Retry}"
                        Style="{StaticResource ArchiveRetryButtonStyle}"
                        Visibility="{Binding IsFailed, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>

                <Border
                    Grid.Row="1"
                    Margin="0,22,0,0"
                    Padding="12,14,12,6"
                    MinHeight="340"
                    Background="{StaticResource InsetBackgroundBrush}"
                    BorderBrush="{StaticResource FieldBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14">
                    <Grid>
                        <ListBox
                            ItemContainerStyle="{StaticResource ArchiveListBoxItemStyle}"
                            ItemsSource="{Binding BirdsView}">
                            <ListBox.Style>
                                <Style BasedOn="{StaticResource ArchiveListBoxStyle}" TargetType="ListBox">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BirdsView.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type dto:BirdDTO}">
                                    <local:BirdItemControl
                                        DataContext="{Binding Converter={StaticResource BirdVmConverter}}">
                                        <local:BirdItemControl.DisplayIndex>
                                            <MultiBinding Converter="{StaticResource AlternationIndexDisplayConverter}">
                                                <Binding RelativeSource="{RelativeSource AncestorType=ListBoxItem}"
                                                         Path="Content" />
                                                <Binding RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                            </MultiBinding>
                                        </local:BirdItemControl.DisplayIndex>
                                    </local:BirdItemControl>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BirdsView.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock
                                HorizontalAlignment="Center"
                                Foreground="{StaticResource PrimaryTextBrush}"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="{loc:Loc Key=BirdList.EmptyTitle}" />
                            <TextBlock
                                Margin="0,8,0,0"
                                HorizontalAlignment="Center"
                                Foreground="{StaticResource SecondaryTextBrush}"
                                FontSize="13"
                                Text="{loc:Loc Key=BirdList.EmptyDescription}" />
                        </StackPanel>

                        <Grid
                            Background="#9A10161D"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <ProgressBar
                                    Width="180"
                                    Height="8"
                                    IsIndeterminate="True" />
                                <TextBlock
                                    Margin="0,12,0,0"
                                    HorizontalAlignment="Center"
                                    Foreground="{StaticResource PrimaryTextBrush}"
                                    FontSize="14"
                                    Text="{loc:Loc Key=BirdList.Loading}" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

