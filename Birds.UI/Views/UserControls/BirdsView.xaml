<UserControl
    x:Class="Birds.UI.Views.UserControls.BirdsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Birds.UI.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:BirdListViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Border
        Margin="20"
        Padding="20"
        Background="#252535"
        CornerRadius="20">

        <DockPanel>

            <!-- Количество птиц -->
            <StackPanel
                Margin="0,0,0,10"
                DockPanel.Dock="Top"
                Orientation="Horizontal">
                <TextBlock
                    DockPanel.Dock="Top"
                    FontSize="24"
                    Text="{Binding BirdsView.Count,
                                   StringFormat=Всего птиц: {0},
                                   Mode=OneWay}" />
                <ComboBox
                    DisplayMemberPath="Display"
                    ItemsSource="{Binding Filters}"
                    SelectedItem="{Binding SelectedFilter,
                                           Mode=TwoWay}"
                    Style="{StaticResource FilterComboBoxStyle}" />
            </StackPanel>

            <ScrollViewer
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding BirdsView}">

                    <!-- Растягиваем контейнер каждого элемента под ширину вьюпорта -->
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="HorizontalAlignment"
                                    Value="Stretch" />
                            <Setter Property="Width"
                                    Value="{Binding RelativeSource={RelativeSource AncestorType=ScrollContentPresenter},
                                        Path=ActualWidth}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <!-- Панель раскладки (можно VirtualizingStackPanel) -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                            <!-- вертикальный список -->
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:BirdViewModel}">

                            <Border
                                Margin="0,5,20,5"
                                Padding="10"
                                HorizontalAlignment="Stretch"
                                Background="#2D2D3D"
                                CornerRadius="10">

                                <Grid HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Название / описание -->
                                    <StackPanel
                                        Grid.Column="0"
                                        Margin="5"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Orientation="Vertical">
                                        <TextBlock
                                            HorizontalAlignment="Left"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Text="{Binding Name}" />
                                        <TextBlock
                                            Margin="0,2,20,2"
                                            HorizontalAlignment="Left"
                                            FontSize="14"
                                            Foreground="#BBB"
                                            Text="{Binding Description}" />
                                    </StackPanel>

                                    <!-- Даты -->
                                    <StackPanel
                                        Grid.Column="1"
                                        Margin="5"
                                        Orientation="Horizontal">
                                        <TextBlock
                                            Margin="0,0,5,0"
                                            Text="{Binding Arrival,
                                                           StringFormat={}{0:dd.MM.yyyy},
                                                           ConverterCulture=ru-ru}" />
                                        <TextBlock
                                            Margin="0,0,5,0"
                                            Text="—" />
                                        <TextBlock Text="{Binding Departure, StringFormat={}{0:dd.MM.yyyy}, ConverterCulture=ru-ru}" />
                                    </StackPanel>

                                    <ContentControl
                                        Grid.Column="2"
                                        Content="{Binding}">
                                        <ContentControl.Style>
                                            <Style TargetType="ContentControl">
                                                <Style.Triggers>
                                                    <!-- шаблон для обычных кнопок -->
                                                    <DataTrigger
                                                        Binding="{Binding IsConfirmingDelete}"
                                                        Value="False">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource NormalButtonsTemplate}" />
                                                    </DataTrigger>

                                                    <!-- шаблон для подтверждения -->
                                                    <DataTrigger
                                                        Binding="{Binding IsConfirmingDelete}"
                                                        Value="True">
                                                        <Setter Property="ContentTemplate" Value="{StaticResource ConfirmButtonsTemplate}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ContentControl.Style>
                                    </ContentControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>