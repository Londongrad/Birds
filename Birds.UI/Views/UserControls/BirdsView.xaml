<UserControl
    x:Class="Birds.UI.Views.UserControls.BirdsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:Birds.UI.Services.Localization"
    xmlns:dto="clr-namespace:Birds.Application.DTOs;assembly=Birds.Application"
    xmlns:local="clr-namespace:Birds.UI.Views.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Birds.UI.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:BirdListViewModel}"
    d:DesignHeight="720"
    d:DesignWidth="980"
    mc:Ignorable="d">

    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{DynamicResource ThemeCardBackgroundColor}" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="{DynamicResource ThemeCardBorderColor}" />
        <SolidColorBrush x:Key="InsetBackgroundBrush" Color="{DynamicResource ThemeInsetBackgroundColor}" />
        <SolidColorBrush x:Key="FieldBackgroundBrush" Color="{DynamicResource ThemeFieldBackgroundColor}" />
        <SolidColorBrush x:Key="FieldBorderBrush" Color="{DynamicResource ThemeFieldBorderColor}" />
        <SolidColorBrush x:Key="FieldBorderHoverBrush" Color="{DynamicResource ThemeFieldBorderHoverColor}" />
        <SolidColorBrush x:Key="FieldBorderFocusBrush" Color="{DynamicResource ThemeFieldBorderFocusColor}" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="{DynamicResource ThemePrimaryTextColor}" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{DynamicResource ThemeSecondaryTextColor}" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="{DynamicResource ThemeMutedTextColor}" />
        <SolidColorBrush x:Key="AccentButtonBrush" Color="{DynamicResource ThemeAccentButtonColor}" />
        <SolidColorBrush x:Key="AccentButtonHoverBrush" Color="{DynamicResource ThemeAccentButtonHoverColor}" />
        <SolidColorBrush x:Key="AccentButtonBorderBrush" Color="{DynamicResource ThemeAccentButtonBorderColor}" />

        <Style x:Key="ArchiveSearchBoxStyle" TargetType="TextBox">
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource FieldBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource FieldBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border
                            x:Name="FieldBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <Grid Margin="14,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Path
                                    Grid.Column="0"
                                    Width="14"
                                    Height="14"
                                    Margin="0,0,12,0"
                                    VerticalAlignment="Center"
                                    Data="M 6 1.5 A 4.5 4.5 0 1 1 5.999 1.5 M 9.3 9.3 L 13 13"
                                    Stroke="{StaticResource MutedTextBrush}"
                                    StrokeStartLineCap="Round"
                                    StrokeEndLineCap="Round"
                                    StrokeThickness="1.6" />

                                <Grid Grid.Column="1">
                                    <ScrollViewer
                                        x:Name="PART_ContentHost"
                                        Margin="0"
                                        VerticalAlignment="Center" />
                                    <TextBlock
                                        x:Name="PlaceholderText"
                                        VerticalAlignment="Center"
                                        Foreground="{StaticResource MutedTextBrush}"
                                        IsHitTestVisible="False"
                                        Visibility="Collapsed"
                                        Text="{loc:Loc Key=BirdList.SearchPlaceholder}" />
                                </Grid>

                                <Button
                                    x:Name="ClearButton"
                                    Grid.Column="2"
                                    Width="22"
                                    Height="22"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Command="{TemplateBinding Tag}"
                                    Cursor="Hand"
                                    Focusable="False"
                                    Foreground="{StaticResource MutedTextBrush}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border
                                                x:Name="ButtonBorder"
                                                Background="Transparent"
                                                CornerRadius="11">
                                                <Path
                                                    Width="8"
                                                    Height="8"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 1 1 L 7 7 M 7 1 L 1 7"
                                                    Stroke="{TemplateBinding Foreground}"
                                                    StrokeStartLineCap="Round"
                                                    StrokeEndLineCap="Round"
                                                    StrokeThickness="1.5" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#16222E" />
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.85" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource FieldBorderHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource FieldBorderFocusBrush}" />
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArchiveFilterItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border
                            x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1A2028" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#232C37" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArchiveFilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="MaxDropDownHeight" Value="340" />
            <Setter Property="Padding" Value="40,0,42,0" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource FieldBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource FieldBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource ArchiveFilterItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border
                                x:Name="FieldBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10" />

                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                IsHitTestVisible="False" />

                            <Path
                                Width="14"
                                Height="14"
                                Margin="14,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Data="M 1 2 L 13 2 L 8 7 L 8 12 L 6 11 L 6 7 Z"
                                Fill="{StaticResource MutedTextBrush}"
                                Opacity="0.8" />

                            <Path
                                Width="11"
                                Height="7"
                                Margin="0,0,16,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Data="M 0 0 L 5.5 5.5 L 11 0"
                                Stroke="#96A3B3"
                                StrokeThickness="1.6" />

                            <ToggleButton
                                Background="Transparent"
                                BorderThickness="0"
                                Focusable="False"
                                IsChecked="{Binding IsDropDownOpen,
                                                    Mode=TwoWay,
                                                    RelativeSource={RelativeSource TemplatedParent}}"
                                Opacity="0" />

                            <Popup
                                x:Name="PART_Popup"
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                Placement="Bottom"
                                PopupAnimation="Fade">
                                <Border
                                    Margin="0,6,0,0"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Padding="8,8,6,8"
                                    Background="{StaticResource InsetBackgroundBrush}"
                                    BorderBrush="{StaticResource FieldBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10">
                                    <ScrollViewer
                                        CanContentScroll="False"
                                        HorizontalScrollBarVisibility="Disabled"
                                        VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource FieldBorderHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{StaticResource FieldBorderFocusBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArchiveRetryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="MinWidth" Value="136" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource AccentButtonBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentButtonBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentButtonHoverBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentButtonBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.94" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ArchiveListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        </Style>

        <Style x:Key="ArchiveListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24,12,24,24">
        <Border
            MaxWidth="980"
            Padding="28"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0"
                        Margin="0,0,14,0"
                        MinWidth="240"
                        Style="{StaticResource ArchiveSearchBoxStyle}"
                        Tag="{Binding ClearSearchCommand}"
                        Text="{Binding SearchText, TargetNullValue='', UpdateSourceTrigger=PropertyChanged}" />

                    <Border
                        Grid.Column="1"
                        Margin="0,0,14,0"
                        MinHeight="46"
                        MinWidth="132"
                        Padding="14,0"
                        VerticalAlignment="Center"
                        Background="{StaticResource InsetBackgroundBrush}"
                        BorderBrush="{StaticResource FieldBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource MutedTextBrush}"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Text="{loc:Loc Key=BirdList.CountTitle}" />
                            <TextBlock
                                Grid.Column="1"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource PrimaryTextBrush}"
                                FontSize="20"
                                FontWeight="Bold"
                                Text="{Binding BirdCount, Mode=OneWay}" />
                        </Grid>
                    </Border>

                    <ComboBox
                        Grid.Column="2"
                        MinWidth="200"
                        MaxWidth="240"
                        DisplayMemberPath="Display"
                        ItemsSource="{Binding Filters}"
                        SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                        Style="{StaticResource ArchiveFilterComboBoxStyle}" />

                    <Button
                        Grid.Column="3"
                        Margin="14,0,0,0"
                        Command="{Binding ReloadBirdsCommand}"
                        Content="{loc:Loc Key=BirdList.Retry}"
                        Style="{StaticResource ArchiveRetryButtonStyle}"
                        Visibility="{Binding IsFailed, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>

                <Border
                    Grid.Row="1"
                    Margin="0,22,0,0"
                    Padding="12,14,12,6"
                    MinHeight="340"
                    Background="{StaticResource InsetBackgroundBrush}"
                    BorderBrush="{StaticResource FieldBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14">
                    <Grid>
                        <ListBox
                            ItemContainerStyle="{StaticResource ArchiveListBoxItemStyle}"
                            ItemsSource="{Binding BirdsView}">
                            <ListBox.Style>
                                <Style BasedOn="{StaticResource ArchiveListBoxStyle}" TargetType="ListBox">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BirdsView.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type dto:BirdDTO}">
                                    <local:BirdItemControl DataContext="{Binding Converter={StaticResource BirdVmConverter}}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BirdsView.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock
                                HorizontalAlignment="Center"
                                Foreground="{StaticResource PrimaryTextBrush}"
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="{loc:Loc Key=BirdList.EmptyTitle}" />
                            <TextBlock
                                Margin="0,8,0,0"
                                HorizontalAlignment="Center"
                                Foreground="{StaticResource SecondaryTextBrush}"
                                FontSize="13"
                                Text="{loc:Loc Key=BirdList.EmptyDescription}" />
                        </StackPanel>

                        <Grid
                            Background="#9A10161D"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <ProgressBar
                                    Width="180"
                                    Height="8"
                                    IsIndeterminate="True" />
                                <TextBlock
                                    Margin="0,12,0,0"
                                    HorizontalAlignment="Center"
                                    Foreground="{StaticResource PrimaryTextBrush}"
                                    FontSize="14"
                                    Text="{loc:Loc Key=BirdList.Loading}" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
